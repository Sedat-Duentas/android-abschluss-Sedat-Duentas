package com.example.laenderapp.data.datamodels

// Hier wird eine Continent Klasse deklariert, mit den einzelnen Variablen, woraus er besteht,
// mit dem Int werden sie in den Ressourcen aufgerufen
data class Continent (
    val stringResource: Int,
    val imageResource: Int
)